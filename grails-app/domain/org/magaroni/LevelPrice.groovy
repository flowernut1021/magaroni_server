package org.magaroni

/**
 * TODO: could I use a enum, don't use a table.
 */
class LevelPrice {

    /**
     *
     item_detail_Id	varchar(50) //foreign key
     price_of_l1	double
     price_of_l2	double
     price_of_l3	double

     */
    static constraints = {
    }
}
